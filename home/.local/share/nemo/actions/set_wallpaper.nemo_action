[Nemo Action]

Active=true
Name=Set as wallpaper
Comment=Set current image as wallpaper
Exec=sh -c "${XDG_BIN_HOME:-$HOME/.local/bin}/set-wallpaper.sh %F"
Icon-Name=image
Selection=single;
Mimetypes=image/*;
Quote=double
