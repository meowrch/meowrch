#!/bin/bash
#
# Meowrch Update Checker - система управления обновлениями
#

set -euo pipefail

# Константы
readonly GITHUB_API_URL="https://api.github.com/repos/meowrch/meowrch/releases"
readonly VERSION_FILE="/usr/local/share/meowrch/users/$(whoami)/version"
readonly NOTIFICATION_STATE="$HOME/.local/state/meowrch-notifications"

# Цвета
readonly GREEN='\033[0;32m'
readonly YELLOW='\033[1;33m'
readonly RED='\033[0;31m'
readonly NC='\033[0m'

# Функции вывода
log() {
    echo -e "${GREEN}[INFO]${NC} $1" >&2
}

warn() {
    echo -e "${YELLOW}[WARN]${NC} $1" >&2
}

error() {
    echo -e "${RED}[ERROR]${NC} $1" >&2
}

# Получить текущую версию
get_current_version() {
    if [[ -f "$VERSION_FILE" ]]; then
        cat "$VERSION_FILE"
    else
        echo "2.0.0"  # По умолчанию
    fi
}

# Получить последнюю версию с GitHub
get_latest_version() {
    curl -s "$GITHUB_API_URL" | jq -r '.[0].tag_name' | sed 's/^v//'
}

# Найти последнюю версию с файлом миграции
get_latest_version_with_migration() {
    local releases
    releases=$(curl -s "$GITHUB_API_URL")
    
    # Проходим по релизам от новых к старым и ищем первый с миграционным файлом
    for version in $(echo "$releases" | jq -r '.[].tag_name' | sed 's/^v//'); do
        local migration_exists
        migration_exists=$(echo "$releases" | jq --arg version "v$version" --arg version_no_v "$version" --arg filename "migrate_to_$version.py" '
            .[] | select(.tag_name == $version or .tag_name == $version_no_v) | 
            .assets[] | select(.name == $filename) | .name'
        )
        
        if [[ -n "$migration_exists" ]]; then
            echo "$version"
            return 0
        fi
    done
    
    # Если не найдено ни одной версии с миграцией
    return 1
}

# Получить все релизы, отсортированные по версии
get_all_releases() {
    curl -s "$GITHUB_API_URL" | jq -r '.[].tag_name' | sed 's/^v//' | sort -V
}

# Установить версию
set_version() {
    local version="$1"
    sudo mkdir -p "$(dirname "$VERSION_FILE")"
    echo "$version" | sudo tee "$VERSION_FILE" > /dev/null
    log "✅ Версия обновлена до $version"
}

# Сравнение версий (v1 > v2)
version_gt() {
    printf '%s\n%s\n' "$2" "$1" | sort -V -C
}

# Показать содержимое файла миграции пользователю
show_migration_preview() {
    local migration_file="$1"
    local version="$2"
    
    log "📋 Просмотр файла миграции migrate_to_$version.py"
    log "💡 Используйте стрелки для навигации, 'q' для выхода из просмотра"
    echo "" >&2
    
    # Создаем временный файл с заголовком для красивого отображения
    local temp_preview="/tmp/migration-preview-$$"
    {
        echo "==================== MIGRATION PREVIEW ===================="
        echo "Файл: migrate_to_$version.py"
        echo "Версия: $version"
        echo "=========================================================="
        echo ""
        cat "$migration_file"
        echo ""
        echo "=========================================================="
        echo "Конец файла миграции"
    } > "$temp_preview"
    
    # Показываем файл через less с настройками как в paru
    if command -v less > /dev/null 2>&1; then
        # Настройки less:
        # -R: отображение ANSI цветов
        # -S: не переносить длинные строки
        # -F: автоматически выходить если содержимое помещается на экран
        # -X: не очищать экран при выходе
        # +Gg: перейти в конец файла и показать номер строки
        export LESS="-R -S -F -X"
        
        # Показываем превью через less
        less "$temp_preview"
    else
        # Фоллбэк для систем без less
        warn "less не найден, показываем содержимое через cat"
        cat "$temp_preview" >&2
    fi
    
    # Удаляем временный файл
    rm -f "$temp_preview"
    
    echo "" >&2
    
    # Спрашиваем пользователя о продолжении
    while true; do
        echo -ne "${YELLOW}==> Продолжить выполнение миграции? [Y/n]: ${NC}" >&2
        read -r response
        case "$response" in
            ""|[Yy]|[Yy][Ee][Ss]|[Дд]|[Дд][Аа])
                return 0
                ;;
            [Nn]|[Nn][Oo]|[Нн]|[Нн][Ее][Тт])
                return 1
                ;;
            *)
                echo -e "${RED}Пожалуйста, введите Y (да) или N (нет)${NC}" >&2
                ;;
        esac
    done
}

# Скачать и выполнить миграцию
run_migration() {
    local version="$1"
    local temp_dir="/tmp/meowrch-update-$version"
    
    # Удаляем старую папку если существует
    rm -rf "$temp_dir"
    mkdir -p "$temp_dir"
    
    log "🔄 Проверяем обновление на версию $version..."
    
    # Получаем данные релиза (пробуем и с префиксом v, и без)
    local release_data
    release_data=$(curl -s "$GITHUB_API_URL" | jq --arg version "v$version" --arg version_no_v "$version" '.[] | select(.tag_name == $version or .tag_name == $version_no_v)')
    
    if [[ -z "$release_data" || "$release_data" == "null" ]]; then
        error "Релиз версии $version не найден"
        rm -rf "$temp_dir"
        return 1
    fi
    
    # СНАЧАЛА проверяем, есть ли файл миграции
    local migration_download_url
    migration_download_url=$(echo "$release_data" | jq -r --arg filename "migrate_to_$version.py" '.assets[] | select(.name == $filename) | .browser_download_url')
    
    if [[ -z "$migration_download_url" || "$migration_download_url" == "null" ]]; then
        log "📝 Файл миграции migrate_to_$version.py не найден для версии $version"
        log "⏭️ Пропускаем эту версию (версия НЕ обновляется)"
        rm -rf "$temp_dir"
        return 0
    fi
    
    # Только если файл миграции существует - скачиваем архив
    log "✅ Найден файл миграции для версии $version"
    
    # Получаем URL TAR.GZ архива релиза (tarball обычно меньше по размеру)
    local tarball_url
    tarball_url=$(echo "$release_data" | jq -r '.tarball_url')
    
    if [[ -z "$tarball_url" || "$tarball_url" == "null" ]]; then
        error "TAR.GZ архив для версии $version не найден"
        rm -rf "$temp_dir"
        return 1
    fi
    
    log "📦 Скачиваем TAR.GZ архив релиза $version..."
    local tar_file="$temp_dir/release.tar.gz"
    
    # Скачиваем с прогресс-баром если есть поддержка
    if [[ -t 2 ]]; then  # Проверяем что stderr это терминал
        if ! curl -L --progress-bar "$tarball_url" -o "$tar_file"; then
            error "Не удалось скачать TAR.GZ архив релиза"
            rm -rf "$temp_dir"
            return 1
        fi
    else
        # Без прогресс-бара для скриптов/автоматизации
        if ! curl -L -s "$tarball_url" -o "$tar_file"; then
            error "Не удалось скачать TAR.GZ архив релиза"
            rm -rf "$temp_dir"
            return 1
        fi
    fi
    
    log "📂 Распаковываем архив..."
    if ! tar -xzf "$tar_file" -C "$temp_dir"; then
        error "Не удалось распаковать TAR.GZ архив"
        rm -rf "$temp_dir"
        return 1
    fi
    
    # Находим распакованную папку (GitHub создает папку вида username-reponame-commitid)
    # Исключаем саму родительскую папку temp_dir
    local extracted_dir
    extracted_dir=$(find "$temp_dir" -maxdepth 1 -type d -name "*meowrch*" ! -path "$temp_dir" | head -n1)
    
    if [[ -z "$extracted_dir" ]]; then
        error "Не удалось найти распакованную папку релиза"
        log "Содержимое $temp_dir:"
        ls -la "$temp_dir" >&2
        rm -rf "$temp_dir"
        return 1
    fi
    
    log "🔄 Найдена распакованная папка: $(basename "$extracted_dir")"
    
    # Переименовываем в стандартное имя
    mv "$extracted_dir" "$temp_dir/meowrch"
    
    log "📥 Скачиваем файл миграции..."
    local migration_file="$temp_dir/meowrch/migrate_to_$version.py"
    if ! curl -L -s "$migration_download_url" -o "$migration_file"; then
        error "Не удалось скачать файл миграции"
        rm -rf "$temp_dir"
        return 1
    fi
    
    # Показываем содержимое миграции пользователю
    if ! show_migration_preview "$migration_file" "$version"; then
        warn "Миграция отменена пользователем"
        rm -rf "$temp_dir"
        return 1
    fi
    
    log "🔄 Выполняем миграцию на версию $version..."
    
    # Переходим в папку с релизом для выполнения миграции
    cd "$temp_dir/meowrch" || {
        error "Не удалось перейти в папку релиза"
        rm -rf "$temp_dir"
        return 1
    }
    
    # Выполняем миграцию
    if python3 "migrate_to_$version.py"; then
        log "✅ Миграция на версию $version выполнена успешно"
        set_version "$version"
    else
        error "Миграция на версию $version не удалась"
        cd - >/dev/null
        rm -rf "$temp_dir"
        return 1
    fi
    
    # Возвращаемся в исходную директорию
    cd - >/dev/null
    rm -rf "$temp_dir"
    return 0
}


# Проверить, показывалось ли уведомление для версии
is_version_notified() {
    local version="$1"
    [[ -f "$NOTIFICATION_STATE" ]] && grep -q "^$version$" "$NOTIFICATION_STATE"
}

# Отметить версию как уведомленную
mark_version_notified() {
    local version="$1"
    mkdir -p "$(dirname "$NOTIFICATION_STATE")"
    echo "$version" >> "$NOTIFICATION_STATE"
}

# Отметить версию как скрытую (не показывать уведомления)
mark_version_hidden() {
    local version="$1"
    local hidden_file="$HOME/.local/state/meowrch-hidden"
    mkdir -p "$(dirname "$hidden_file")"
    echo "$version" >> "$hidden_file"
}

# Проверить, скрыта ли версия пользователем
is_version_hidden() {
    local version="$1"
    local hidden_file="$HOME/.local/state/meowrch-hidden"
    [[ -f "$hidden_file" ]] && grep -q "^$version$" "$hidden_file"
}

# Запустить обновление в новом терминале
open_update_terminal() {
    # Список терминалов для Meowrch в порядке предпочтения
    local terminals=(
        "kitty"             # Основной терминал в Meowrch
        "alacritty"         # Альтернативный современный
        "gnome-terminal"    # GNOME совместимость
        "xterm"             # Fallback
    )
    
    # Находим доступный терминал
    local terminal_cmd=""
    local terminal_name=""
    for terminal in "${terminals[@]}"; do
        if command -v "$terminal" > /dev/null 2>&1; then
            terminal_name="$terminal"
            case "$terminal" in
                "kitty"|"alacritty")
                    terminal_cmd="$terminal -e"
                    ;;
                "gnome-terminal")
                    terminal_cmd="$terminal --"
                    ;;
                "xterm")
                    terminal_cmd="xterm -e"
                    ;;
            esac
            break
        fi
    done
    
    if [[ -z "$terminal_cmd" ]]; then
        error "Не найден подходящий эмулятор терминала"
        log "💡 Запустите обновление вручную: update-meowrch update"
        return 1
    fi
    
    log "🖥️ Открываем терминал для обновления ($terminal_name)..."
    
    # Создаем временный скрипт для красивого вывода в терминале
    local temp_script="/tmp/meowrch-update-terminal-$$"
    local script_path="$(which update-meowrch 2>/dev/null || echo "/usr/bin/update-meowrch")"
    
    # используем текущий скрипт если не найден в PATH
    if [[ "$script_path" == "/usr/bin/update-meowrch" ]] && [[ ! -f "$script_path" ]]; then
        script_path="${0}"
    fi
    
    # Экранируем переменную script_path для использования в heredoc
    local script_path_escaped="${script_path}"
    
    cat > "$temp_script" << 'EOF'
#!/bin/bash
set -e

# Путь к скрипту обновления
SCRIPT_PATH="@SCRIPT_PATH@"

# Цвета
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m'

echo -e "${BLUE}═══════════════════════════════════════════════════════════════${NC}"
echo -e "${CYAN}             🔄 ОБНОВЛЕНИЕ СИСТЕМЫ MEOWRCH 🔄${NC}"
echo -e "${BLUE}═══════════════════════════════════════════════════════════════${NC}"
echo ""
echo -e "${YELLOW}Начинаем процесс обновления...${NC}"
echo ""

# Запускаем обновление
if "${SCRIPT_PATH}" update; then
    echo ""
    echo -e "${GREEN}═══════════════════════════════════════════════════════════════${NC}"
    echo -e "${GREEN}             ✅ ОБНОВЛЕНИЕ ЗАВЕРШЕНО УСПЕШНО! ✅${NC}"
    echo -e "${GREEN}═══════════════════════════════════════════════════════════════${NC}"
    echo ""
    echo -e "${GREEN}🎉 Meowrch был успешно обновлен!${NC}"
    echo -e "${YELLOW}💡 Рекомендуется перезагрузить систему для применения всех изменений${NC}"
else
    echo ""
    echo -e "${RED}═══════════════════════════════════════════════════════════════${NC}"
    echo -e "${RED}             ❌ ОШИБКА ПРИ ОБНОВЛЕНИИ! ❌${NC}"
    echo -e "${RED}═══════════════════════════════════════════════════════════════${NC}"
    echo ""
    echo -e "${RED}💥 Произошла ошибка при обновлении${NC}"
    echo -e "${YELLOW}💡 Проверьте логи выше или запустите '${SCRIPT_PATH} status' для диагностики${NC}"
fi

echo ""
echo -e "${BLUE}Нажмите Enter для закрытия терминала...${NC}"
read

# Удаляем временный скрипт
rm -f "$0"
EOF
    
    # Заменяем маркер @SCRIPT_PATH@ на реальный путь к скрипту
    sed -i "s|@SCRIPT_PATH@|${script_path_escaped}|g" "$temp_script"
    
    chmod +x "$temp_script"
    
    # Запускаем в фоновом режиме
    nohup $terminal_cmd bash "$temp_script" > /dev/null 2>&1 &
    
    log "✅ Терминал с обновлением запущен"
}

notify_user() {
    local latest_version="$1"
    local current_version="$2"
    
    # Определяем иконку
    local icon_arg=""
    if [[ -f "/usr/share/pixmaps/meowrch-logo.png" ]]; then
        icon_arg="-i /usr/share/pixmaps/meowrch-logo.png"
    else
        icon_arg="-i software-update-available"
    fi
    
    # Desktop уведомление с кнопками
    if command -v notify-send > /dev/null 2>&1; then
        # Проверяем поддержку действий (actions) в notify-send
        if notify-send --help 2>/dev/null | grep -q -- "--action"; then
            # Современная версия с поддержкой кнопок
            local result
            result=$(notify-send \
                --app-name="Meowrch" \
                "Доступна новая версия!" \
                "Версия $latest_version готова к установке.\nДля обновления запустите: update-meowrch update" \
                $icon_arg \
                --urgency="critical" \
                --expire-time=0 \
                --action="update=Обновить сейчас" \
                --action="hide=Больше не показывать" \
                --action="later=Напомнить позже" 2>/dev/null || echo "")
            
            # Обрабатываем ответ пользователя
            case "$result" in
                "update")
                    log "🚀 Пользователь запустил обновление из уведомления"
                    open_update_terminal
                    ;;
                "hide")
                    log "🔕 Пользователь скрыл уведомления для версии $latest_version"
                    mark_version_hidden "$latest_version"
                    ;;
                "later"|"")
                    log "⏰ Пользователь отложил обновление"
                    ;;
            esac
        else
            # Простое уведомление без кнопок
            notify-send \
                --app-name="Meowrch" \
                "🔄 Новая версия Meowrch!" \
                "Доступна версия $latest_version (текущая: $current_version)\n\nДля обновления: update-meowrch update" \
                $icon_arg \
                --urgency="critical" \
                --expire-time=0
        fi
    fi
    
    log "🆕 Доступна новая версия: $latest_version (текущая: $current_version)"
    log "💡 Для обновления используйте: update-meowrch update"
}

check_update() {
    local current_version
    current_version=$(get_current_version)
    
    log "Проверяем обновления... (текущая версия: $current_version)"
    
    local latest_with_migration
    latest_with_migration=$(get_latest_version_with_migration)
    
    if [[ -z "$latest_with_migration" ]]; then
        log "✅ Нет доступных обновлений (нет версий с миграционными файлами)"
        return 0
    fi

    if version_gt "$latest_with_migration" "$current_version"; then
        # Проверяем, скрыта ли версия пользователем
        if is_version_hidden "$latest_with_migration"; then
            log "ℹ️ Уведомления для версии $latest_with_migration скрыты пользователем"
            return 0
        fi
        
        # Проверяем, показывали ли уже уведомление (но только если не скрыто)
        if is_version_notified "$latest_with_migration"; then
            # Показываем уведомление повторно, так как пользователь не скрыл его
            log "🔄 Показываем повторное уведомление для версии $latest_with_migration"
            notify_user "$latest_with_migration" "$current_version"
        else
            # Первый показ уведомления
            log "🆕 Показываем уведомление о новой версии $latest_with_migration"
            notify_user "$latest_with_migration" "$current_version"
            mark_version_notified "$latest_with_migration"
        fi
    else
        log "✅ У вас последняя доступная версия ($current_version)"
    fi
}

# Показать статус текущей версии
show_status() {
    local current_version
    current_version=$(get_current_version)
    local latest_version
    latest_version=$(get_latest_version)
    local latest_with_migration
    latest_with_migration=$(get_latest_version_with_migration)
    
    log "📋 Текущая версия: $current_version"
    log "📋 Последняя версия в GitHub: $latest_version"
    
    if [[ -n "$latest_with_migration" ]]; then
        log "📋 Последняя версия с миграцией: $latest_with_migration"
        
        if [[ "$current_version" == "$latest_with_migration" ]]; then
            log "✅ У вас последняя версия с доступными обновлениями"
        elif version_gt "$latest_with_migration" "$current_version"; then
            log "🆕 Доступно обновление до версии $latest_with_migration"
        else
            log "ℹ️ Ваша версия новее последней версии с миграцией"
        fi
    else
        log "ℹ️ Нет доступных версий с файлами миграции"
    fi
}

# Выполнить обновление
perform_update() {
    local current_version
    current_version=$(get_current_version)
    
    # Теперь обновляемся только до последней версии с миграцией
    local target_version
    target_version=$(get_latest_version_with_migration)
    
    if [[ -z "$target_version" ]]; then
        warn "Нет доступных версий с миграционными файлами"
        return 0
    fi

    log "🔄 Обновление с $current_version до $target_version"

    if [[ "$current_version" == "$target_version" ]]; then
        log "✅ Версия $target_version уже установлена"
        return 0
    fi
    
    if ! version_gt "$target_version" "$current_version"; then
        log "ℹ️ Ваша версия ($current_version) не старше последней доступной ($target_version)"
        return 0
    fi

    log "📋 Прямое обновление: $current_version → $target_version"
    log "💡 Миграция полностью перезапишет конфигурацию с бэкапом предыдущей"

    # Выполняем прямое обновление на последнюю версию с миграцией
    if ! run_migration "$target_version"; then
        error "Обновление не удалось"
        return 1
    fi

    log "🎉 Обновление успешно применено!"
}

# Показать справку
show_help() {
    cat << EOF
Meowrch Update Checker - система управления обновлениями

ИСПОЛЬЗОВАНИЕ:
    $0 COMMAND

КОМАНДЫ:
    check-update     Проверить наличие обновлений и уведомить
    status          Показать статус версий
    update          Обновиться до последней доступной версии
    help            Показать эту справку

ПРИМЕРЫ:
    $0 check-update      # Проверить обновления
    $0 status           # Показать статус
    $0 update           # Обновиться до последней версии

EOF
}

main() {
    # Проверяем обязательные утилиты
    for cmd in curl jq tar python3; do
        if ! command -v "$cmd" > /dev/null 2>&1; then
            error "Требуется утилита: $cmd"
            exit 1
        fi
    done
    
    # Проверяем less (не критично, но желательно для удобства)
    if ! command -v less > /dev/null 2>&1; then
        warn "Утилита 'less' не найдена. Файлы миграции будут показаны через 'cat'"
    fi

    local command="${1:-check-update}"
    
    case "$command" in
        check-update)
            check_update
            ;;
        status)
            show_status
            ;;
        update)
            perform_update
            ;;
        help|--help|-h)
            show_help
            ;;
        *)
            error "Неизвестная команда: $command"
            show_help
            exit 1
            ;;
    esac
}

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
